"""
Scenario-based evaluation harness for LearnX.

- corpus: builds the fixed, versioned introductory Python corpus and its index
- scenarios: generates the 24 controlled learner scenarios
- conditions: the E1 ablation conditions (A1 to A5)
- single_agent: the A2 single-agent baseline
- runner: drives the system over scenarios and writes result files
"""
